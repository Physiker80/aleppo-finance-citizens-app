# 📱 تطبيق المحمول للمواطنين - دليل الإعداد

## 🎯 نظرة عامة

تم إنشاء تطبيق محمول مخصص للمواطنين يتضمن الميزات التالية:
- **الصفحة الرئيسية**: عرض المعلومات والأخبار
- **تقديم طلب**: إرسال استعلامات وشكاوى
- **متابعة الطلب**: تتبع حالة الطلبات المقدمة
- **حجز موعد**: جدولة مواعيد مع الموظفين
- **الأسئلة الشائعة**: إجابات على الاستفسارات المتكررة
- **اتصل بنا**: معلومات التواصل

## 🏗️ الهيكلية

### الملفات المضافة

| الملف | الوصف |
|-------|------|
| `utils/platform.ts` | كشف المنصة (Android/iOS/Web) |
| `components/mobile/MobileDrawer.tsx` | القائمة الجانبية |
| `components/mobile/MobileHeader.tsx` | الشريط العلوي |
| `components/mobile/MobileLayout.tsx` | التخطيط الرئيسي |
| `services/mobilePushNotifications.ts` | إشعارات Firebase |
| `services/offlineStorage.ts` | التخزين المحلي |
| `hooks/useMobileInit.ts` | تهيئة ميزات المحمول |

### الملفات المعدلة

| الملف | التعديلات |
|-------|----------|
| `App.tsx` | دعم التوجيه للمحمول وفصل صفحات المواطنين |
| `index.css` | دعم Safe Areas وأنماط المحمول |
| `capacitor.config.ts` | إعدادات الإضافات |
| `AndroidManifest.xml` | أذونات التطبيق |
| `public/sw.js` | تحسينات للتخزين المؤقت |

## 🔧 الإضافات المثبتة

```bash
@capacitor/camera@7.0.3
@capacitor/haptics@7.0.3
@capacitor/preferences@7.0.3
@capacitor/push-notifications@7.0.4
```

## 📲 البناء والنشر

### بناء التطبيق

```bash
# بناء الويب
npm run build

# مزامنة مع Android
npx cap sync android

# فتح Android Studio
npx cap open android
```

### تشغيل للتطوير

```bash
# تشغيل خادم التطوير
npm run dev

# تشغيل على المحاكي (بعد تفعيل CAP_USE_DEV_SERVER=1)
npx cap run android
```

## ⚙️ إعدادات Firebase (للإشعارات)

### الحالة الحالية:
✅ تم إضافة Firebase BOM و Messaging في `build.gradle`  
✅ تم إنشاء ملف نموذجي `google-services.json.template`  
⏳ يتبقى: تحميل ملف `google-services.json` الحقيقي من Firebase Console

### الخطوات المطلوبة:

1. **إنشاء مشروع Firebase**:
   - اذهب إلى [Firebase Console](https://console.firebase.google.com/)
   - أنشئ مشروع جديد أو استخدم مشروع موجود

2. **إضافة تطبيق Android**:
   - اضغط على "Add app" → Android
   - Package name: `com.aleppo.finance.system`
   - App nickname: `نظام الاستعلامات والشكاوى`
   - اضغط "Register app"

3. **تحميل google-services.json**:
   - حمّل الملف من Firebase Console
   - ضعه في: `android/app/google-services.json`
   - (احذف أو تجاهل ملف `.template`)

4. **البناء والاختبار**:
   ```bash
   npx cap sync android
   npx cap open android
   # ثم Build → Run في Android Studio
   ```

## 🔐 الأذونات

تم إضافة الأذونات التالية:

| الإذن | الغرض |
|-------|------|
| `INTERNET` | الاتصال بالخادم |
| `ACCESS_NETWORK_STATE` | كشف حالة الاتصال |
| `POST_NOTIFICATIONS` | إرسال إشعارات (Android 13+) |
| `VIBRATE` | اهتزاز عند الإشعارات |
| `CAMERA` | تصوير المرفقات |
| `READ_MEDIA_IMAGES` | قراءة الصور |
| `RECEIVE_BOOT_COMPLETED` | تشغيل عند إعادة التشغيل |
| `FOREGROUND_SERVICE` | خدمات الخلفية |

## 📴 العمل بدون اتصال

### الميزات:
- **تخزين مؤقت للصفحات**: تحميل سريع عند عدم الاتصال
- **طابور الطلبات**: حفظ الطلبات محلياً وإرسالها عند عودة الاتصال
- **مزامنة تلقائية**: Background Sync عند عودة الاتصال
- **مؤشر الحالة**: عرض شريط عند فقدان الاتصال

### استخدام التخزين المحلي:

```typescript
import { addPendingRequest, getPendingCount } from '../services/offlineStorage';

// إضافة طلب للطابور
await addPendingRequest('ticket', {
  title: 'عنوان الطلب',
  description: 'وصف الطلب'
});

// جلب عدد الطلبات المعلقة
const count = await getPendingCount();
```

## 🎨 واجهة المستخدم

### القائمة الجانبية (Drawer)
- تنزلق من اليمين (RTL)
- تحتوي على روابط صفحات المواطنين فقط
- زر تبديل الوضع الليلي/النهاري

### الشريط العلوي (Header)
- زر القائمة
- عنوان الصفحة الحالية
- مؤشر حالة الاتصال

### Safe Areas
- دعم الهواتف ذات الـ Notch
- هوامش مناسبة للمحتوى

## 📱 صفحات المواطنين المتاحة

| المسار | الصفحة |
|--------|-------|
| `#/` أو `#home` | الصفحة الرئيسية |
| `#submit` | تقديم طلب |
| `#track` | متابعة الطلب |
| `#appointment` | حجز موعد |
| `#faq` | الأسئلة الشائعة |
| `#contact` | اتصل بنا |
| `#terms` | الشروط والأحكام |

## 🚫 الصفحات المحظورة على المحمول

جميع صفحات الموظفين والإدارة:
- `/login`
- `/dashboard`
- `/requests`
- `/diwan/*`
- `/hrms/*`
- `/admin/*`

عند محاولة الوصول، يتم إعادة التوجيه للصفحة الرئيسية.

## 🧪 الاختبار

### على المتصفح:
1. افتح DevTools
2. Toggle Device Toolbar (Ctrl+Shift+M)
3. اختر جهاز محمول
4. التطبيق سيكتشف الموبايل ويعرض الواجهة المخصصة

### على المحاكي:
```bash
npx cap run android
```

### على جهاز حقيقي:
1. فعّل USB Debugging على الهاتف
2. وصّل الهاتف بالكمبيوتر
3. شغّل: `npx cap run android --target=<device-id>`

## 📝 ملاحظات

- التطبيق يكتشف تلقائياً إذا كان يعمل على محمول أو متصفح
- على المتصفح العادي، تظهر الواجهة الكاملة بكل الصفحات
- على المحمول، تظهر واجهة مبسطة لصفحات المواطنين فقط
- الإشعارات تتطلب إعداد Firebase بشكل منفصل

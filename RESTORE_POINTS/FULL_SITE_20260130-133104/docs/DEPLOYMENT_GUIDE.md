# 🚀 دليل النشر على المنصات المجانية

## البوابة الإلكترونية لمديرية مالية حلب

---

## 📋 الخيارات المتاحة

| المنصة | التكلفة | SSL | CDN | السعة | الصعوبة |
|--------|---------|-----|-----|-------|---------|
| **GitHub Pages** | مجاني | ✅ | ✅ | غير محدود | ⭐ سهل |
| **Vercel** | مجاني | ✅ | ✅ | 100GB/شهر | ⭐ سهل جداً |
| **Netlify** | مجاني | ✅ | ✅ | 100GB/شهر | ⭐ سهل جداً |
| **Azure SWA** | مجاني | ✅ | ✅ | 100GB/شهر | ⭐⭐ متوسط |

---

## 1️⃣ GitHub Pages (الأسهل)

### الخطوات:

1. **ارفع المشروع على GitHub** (إذا لم يكن موجوداً):
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main
   ```

2. **فعّل GitHub Pages من الإعدادات**:
   - اذهب إلى: `Settings` → `Pages`
   - اختر `Source`: **GitHub Actions**
   - أو اختر Branch: `gh-pages` إذا كنت تستخدم workflow

3. **الملف جاهز**: `.github/workflows/deploy-github-pages.yml`

4. **ادفع التغييرات**:
   ```bash
   git add .
   git commit -m "إضافة GitHub Pages deployment"
   git push
   ```

5. **الرابط**: `https://YOUR_USERNAME.github.io/YOUR_REPO/`

### ⚠️ ملاحظة مهمة لـ GitHub Pages:
يجب تعديل `vite.config.ts` لإضافة base path:

```typescript
export default defineConfig({
  base: '/اسم-المستودع/',  // مثال: '/Aleppo_Finance/'
  // ... باقي الإعدادات
})
```

---

## 2️⃣ Vercel (الأسرع)

### الطريقة 1: من الموقع (بدون كود)

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول بحساب GitHub
3. اضغط "New Project"
4. اختر المستودع من القائمة
5. اضغط "Deploy" - انتهى! ✅

### الطريقة 2: من سطر الأوامر

```bash
# تثبيت Vercel CLI
npm i -g vercel

# النشر
vercel

# للإنتاج
vercel --prod
```

### الملفات الجاهزة:
- ✅ `vercel.json` - إعدادات Vercel

### الرابط:
- `https://your-project.vercel.app`
- يمكنك ربط نطاق مخصص مجاناً

---

## 3️⃣ Netlify (الأكثر ميزات)

### الطريقة 1: من الموقع

1. اذهب إلى [netlify.com](https://netlify.com)
2. سجل دخول بحساب GitHub
3. اضغط "Add new site" → "Import an existing project"
4. اختر المستودع
5. إعدادات البناء:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. اضغط "Deploy site"

### الطريقة 2: من سطر الأوامر

```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# تسجيل الدخول
netlify login

# ربط المشروع
netlify init

# النشر
netlify deploy --prod
```

### الطريقة 3: السحب والإفلات (Drag & Drop)

1. ابنِ المشروع محلياً:
   ```bash
   npm run build
   ```
2. اذهب إلى [app.netlify.com/drop](https://app.netlify.com/drop)
3. اسحب مجلد `dist` وأفلته في الصفحة
4. الموقع جاهز فوراً! ✅

### الملفات الجاهزة:
- ✅ `netlify.toml` - إعدادات Netlify

### الرابط:
- `https://random-name.netlify.app`
- يمكنك تغيير الاسم أو ربط نطاق مخصص

---

## 4️⃣ Azure Static Web Apps (الموجود حالياً)

المشروع منشور حالياً على:
- 🔗 https://ashy-dune-05e306010.6.azurestaticapps.net

---

## 🔄 النشر التلقائي (CI/CD)

جميع المنصات تدعم النشر التلقائي عند كل `git push`:

| المنصة | الملف المطلوب |
|--------|--------------|
| GitHub Pages | `.github/workflows/deploy-github-pages.yml` ✅ |
| Vercel | `vercel.json` ✅ (اختياري) |
| Netlify | `netlify.toml` ✅ |
| Azure SWA | `.github/workflows/azure-static-web-apps-*.yml` ✅ |

---

## 📊 مقارنة الأداء

| المنصة | وقت البناء | وقت النشر | CDN |
|--------|------------|-----------|-----|
| GitHub Pages | ~2 دقيقة | ~1 دقيقة | GitHub CDN |
| Vercel | ~1 دقيقة | ~30 ثانية | Edge Network |
| Netlify | ~1 دقيقة | ~30 ثانية | Global CDN |
| Azure SWA | ~2 دقيقة | ~1 دقيقة | Azure CDN |

---

## 🎯 التوصية

### للاختبار السريع:
استخدم **Netlify Drag & Drop** - ابنِ المشروع واسحب مجلد `dist`

### للإنتاج:
استخدم **Vercel** أو **Azure SWA** للميزات المتقدمة

### للمشاريع مفتوحة المصدر:
استخدم **GitHub Pages** - مجاني بالكامل

---

## 🛠️ أوامر مفيدة

```bash
# بناء المشروع
npm run build

# معاينة البناء محلياً
npm run preview

# النشر على Vercel
npx vercel --prod

# النشر على Netlify
npx netlify deploy --prod --dir=dist
```

---

## 📝 ملاحظات

1. **SPA Routing**: جميع الملفات تحتوي على إعدادات لتوجيه جميع الطلبات إلى `index.html`

2. **HTTPS**: جميع المنصات توفر شهادة SSL مجانية تلقائياً

3. **النطاقات المخصصة**: يمكنك ربط نطاق مخصص مجاناً على جميع المنصات

4. **المتغيرات البيئية**: استخدم لوحة التحكم لكل منصة لإضافة Environment Variables

---

**تاريخ التحديث**: يناير 2026

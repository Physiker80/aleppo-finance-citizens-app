# Local overrides for backend
PORT=4010
CSP_ENABLED=true
CSP_REPORT_ONLY=false
# Optional: disable email in dev to avoid SMTP warnings
EMAIL_ENABLED=false
 
# Optional: protect diagnostics endpoints like /api/csp-violations
# Set a long random value; then pass it as x-api-key header when calling the endpoint.
# METRICS_API_KEY=your-very-long-random-key

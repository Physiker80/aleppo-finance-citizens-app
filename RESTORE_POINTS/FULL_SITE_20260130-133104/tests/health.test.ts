import { describe, it, expect } from 'vitest';

// Placeholder health test (will expand once API endpoints wired to prisma)

describe('health', () => {
  it('basic math still works', () => {
    expect(2 + 3).toBe(5);
  });
});

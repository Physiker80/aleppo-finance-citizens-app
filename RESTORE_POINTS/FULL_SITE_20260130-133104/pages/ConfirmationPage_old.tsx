import React from 'react';

// Legacy file kept for history but replaced with a minimal valid stub so TypeScript is happy.
const ConfirmationPageOld: React.FC = () => {
    return <div style={{ display: 'none' }} />;
};

export default ConfirmationPageOld;